-- Students Table
CREATE TABLE Students (
    StudentID INT IDENTITY(1,1) PRIMARY KEY,
    Name NVARCHAR(100) NOT NULL,
    Section NVARCHAR(20) NOT NULL,
    AcademicYear INT NOT NULL
);

-- Subjects Table
CREATE TABLE Subjects (
    SubjectID INT IDENTITY(1,1) PRIMARY KEY,
    Name NVARCHAR(100) NOT NULL,
    YearOffered INT NOT NULL
);

-- Grades Table
CREATE TABLE Grades (
    GradeID INT IDENTITY(1,1) PRIMARY KEY,
    StudentID INT NOT NULL FOREIGN KEY REFERENCES Students(StudentID),
    SubjectID INT NOT NULL FOREIGN KEY REFERENCES Subjects(SubjectID),
    Assignment1 CHAR(1) CHECK (Assignment1 IN ('P','M','D')),
    Assignment2 CHAR(1) CHECK (Assignment2 IN ('P','M','D')),
    FinalGrade FLOAT NOT NULL
);

-- Sample Data
INSERT INTO Students (Name, Section, AcademicYear) VALUES
('Ahmed Hassan', 'A', 1),
('Sara Ali', 'B', 2);

INSERT INTO Subjects (Name, YearOffered) VALUES
('Mathematics', 1),
('Physics', 2);

INSERT INTO Grades (StudentID, SubjectID, Assignment1, Assignment2, FinalGrade) VALUES
(1, 1, 'P', 'M', 75.5),
(2, 2, 'D', 'D', 92.0);
